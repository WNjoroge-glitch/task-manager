<%- include('partials/header') %>
    
    <div class="container">
        <a class="btn" id="add" href="/create">Add Item</a>
        
        <div class="items">
            <% for(let item of items){ %>
                <p class="item bg-grey"> <a href = "/items/<%=item.id %>"><%= `${item.id}.${item.name}` %></a></p>
    
             <% }; %>
        </div>
        
    </div>

<%- include('partials/footer') %>
    
