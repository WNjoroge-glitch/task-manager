<%- include('partials/header') %>
    
    <div class="container">
        
         <% for(let item of items){ %>
            <p><%= `${item.id}.${item.name}` %></p>

         <% }; %>
    </div>

<%- include('partials/footer') %>
    
